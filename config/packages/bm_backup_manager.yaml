# See full config at https://github.com/backup-manager/symfony
bm_backup_manager:
  database:
    development:
      dsn: '%env(resolve:DATABASE_URL)%'
    production:
      dsn: '%env(resolve:DATABASE_URL)%'
  storage:
    local:
      type: Local
      root: '%kernel.cache_dir%/db'
    ftp:
      type: Ftp
      host: 90.187.101.17
      username: mitscom_int_ftp
      password: ABcd1234
      root: '/FTP-Intern/hoericke'
      port: 21
      passive: false
      ssl: false
      timeout: 30
